# Report 2026-06-07 13:32:27 MSK

**Branch:** main
**Stats:** 1 file changed, 90 insertions(+), 16 deletions(-)

## What was changed

### `scripts/train.py`

- **Per-epoch evaluation**: `MLflowCallback.on_epoch_end` теперь вызывает `evaluate_and_report` после каждой эпохи, а не только в конце обучения. `exact_accuracy`, `char_error_rate`, `number_accuracy` логируются по шагу эпохи (3+ точек в MLflow вместо 1).
- **Evaluation dashboard**: 6 графиков на одной фигуре (`plots/epoch_{epoch}/evaluation_dashboard.png`):
  1. Confusion matrix (digit length)
  2. Length correlation scatter (pred vs target)
  3. Number count histogram (input/target/pred)
  4. Accuracy by input length bucket (color-coded bars)
  5. CER distribution per sample
  6. Prediction length delta histogram
- **MLflowCallback** расширен параметрами: model, tokenizer, val_texts, val_targets, output_dir

## Problems encountered

- MLflow графики имели по одной точке — `evaluate_and_report` вызывался только после всего обучения

## How they were solved

- Вызов per-epoch evaluation добавлен в `on_epoch_end` коллбэка
- Все данные (model, tokenizer, вал. выборка) передаются в коллбэк через конструктор

## Notes for the future

- Обучение всё ещё даёт 2% accuracy — нужно больше эпох (10-15) и весь датасет
- Gradient нормы стабильны (~1-2) — обучения идёт корректно, проблема в недостаточности эпох

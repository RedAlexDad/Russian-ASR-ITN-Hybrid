# Report 2026-06-05 19:15:00 MSK

**Branch:** main

## What was changed

### Все файлы src/*.py и scripts/*.py
Добавлены подробные комментарии на русском языке к каждой части кода:

**src/dicts/*** — объяснение структуры словарей, формат (val, mag),
  зачем нужны падежные формы, что такое порядок величины.

**src/lexicon.py** — описание трёх функций lookup/is_ordinal/ordinal_value,
  алгоритм поиска по словарю и ASR-ошибкам.

**src/parser.py** — полное описание алгоритма группировки:
  сумма (убывающая mag) vs перечисление (неубывающая mag),
  обработка умножителей (тысяча/миллион).

**src/normalizer.py** — пошаговый алгоритм обхода токенов,
  выделения групп и замены.

**src/cli.py** — описание трёх субкоманд run/evaluate/errors.

**src/eda.py** — описание двух режимов отчёта.

**scripts/eda.py** — комментарии к каждой секции EDA,
  описание каждого графика.

**scripts/validate_notebook.py** — полное описание 8 проверок.

# Report 2026-06-05 18:58:00 MSK

**Branch:** main
**Stats:** 2 files changed, 20 insertions(+), 1 deletion(-)

## What was added

### `.dockerignore`
Исключены из контекста сборки:
- `data/`, `answer.f` — тяжёлые бинарные данные
- `__pycache__`, `*.pyc` — мусор
- `.git`, `.gitignore` — служебные файлы
- `reports/plots/*` — сгенерированные PNG (не нужны в образе)

## What was changed

### `Dockerfile`
Было: один `COPY . .` — любое изменение файла инвалидировало весь кэш

Стало: 5 слоёв с разделением по частоте изменений:
1. system deps (apt-get make) — кэш навсегда
2. pip install — кэш до изменения requirements.txt
3. `src/` — кэш до изменения кода
4. `main.py`, `Makefile`, `makefiles/`, `scripts/` — кэш до изменения конфига
5. `reports/` — кэш навсегда

Теперь изменение `src/parser.py` пересобирает только 3-й слой (~0.1 сек),
а не переустанавливает все Python-зависимости.

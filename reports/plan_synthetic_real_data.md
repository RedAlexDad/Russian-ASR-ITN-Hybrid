# План: Генерация синтетических данных для улучшения ITN

**Branch:** main
**Date:** 2026-06-05 22:12:06 MSK

## Мотивация

Текущее решение (97.6% accuracy) упирается в 12 ошибок на calibration.f.
Все ошибки — это паттерны, которых нет или мало в калибровочных данных.
Синтетические данные позволят:
1. Расширить словарь ASR-ошибок (Levenshtein-вариации)
2. Улучшить парсер редких группировок
3. Покрыть слитные написания (дветысячи, двестипятьсот)
4. Возможно, перейти к ML-подходу (seq2seq)

## Анализ текущих ошибок

| Тип ошибки | Кол-во | Описание |
|------------|--------|----------|
| Неверная группировка | 8 | слитные написания, неверное определение границ |
| Пропущенное число | 2 | "тыщ" без предшествующего числа |
| Лишнее число | 2 | "дваста" = 200 (но GT оставляет как есть) |

### 12 конкретных ошибок (для генерации контрпримеров)

1. `тыща четыреста` -> 1400 (слитное "тыща" не в словаре)
2. `дветысячи пятьсот` -> 2500 (слитное "дветысячи" было, но парсер не умножает)
3. `дваста` -> "дваста" (оставить, не нормализовать — расхождение с "двеси")
4. `шестьдесят с чем то тыщ` -> "60 с чем то тыщ" ("тыщ" одиноко)
5. `выше тыщ рублей` -> "выше тыщ рублей" ("тыщ" как существительное)
6. `двадцати тысячам` -> "20 тысячам" (нужно "20000")
7. `двести восемьдесят четвёртый` -> 284 (порядковое не просуммировалось)
8. `семьдесят миллионов два миллиона` -> "70000000 2000000" (два блока)
9. `на четырнадцатое число` -> "14 число" (порядковое не нормализовано)
10. `двести два два` -> "200 2 2" (перечисление, не сумма)
11. `двеси триста пятьдесят тысяч` -> 235000 (ASR-склейка)
12. `пять с половиной тыщ` -> "5 с половиной тыщ" ("тыщ" оставить)

## Поиск датасетов на Hugging Face

Поиск по `russian itn`, `russian text normalization`, `russian numbers` — **0 результатов**.
Готовых датасетов для обратной текстовой нормализации русского языка в открытом доступе нет.

Ближайшие альтернативы (но это ASR, не ITN):
| Датасет | Описание | Проблема |
|---------|----------|----------|
| `SberDevices/golos_crowd` | Русская речь, 1000+ часов | Нужен доступ, нет текстовой нормализации |
| `mozilla-foundation/common_voice` | Многоязычная речь | Русский есть, но нет ITN-разметки |
| `facebook/voxpopuli` | Парламентская речь | Русский есть, только raw text |
| `Darhel/ITNews_ru` | IT-новости на русском | Пустой датасет (заглушка) |

**Вывод:** единственный способ получить ITN-данные — генерировать самому.
Используем комбинацию: синтетика (шаблоны) + реальные данные (Wikipedia + RSS).

## Принцип работы: как мы получаем синтетические данные

### Идея

У нас есть 500 строк calibration.f с эталонной нормализацией. Этого катастрофически мало для обучения любых моделей. Но у нас есть:
1. **Правила русского языка** — мы знаем, как числа превращаются в слова
2. **Паттерны ASR-ошибок** — мы знаем, как слова искажаются
3. **Контекст звонков** — мы знаем типовые фразы (цены, бюджеты, сроки)

Мы берём случайное число, превращаем его в слова, вставляем в контекстную фразу, применяем ASR-шум — и получаем пару `task_text` / `ground_truth`.

### Пайплайн одной пары

```
Число (например, 543)
  │
  ▼
number_to_words(543) → "пятьсот сорок три"
  │
  ▼
Вставка в шаблон: "бюджет в размере {num} рублей"
  → "бюджет в размере пятьсот сорок три рублей"
  │
  ▼
apply_asr_noise():
  ● замена по словарю:          пятьсот → пятьсот (нет в ASR_ERRORS)
  ● склейка соседей (15%):      сорок три → сороктри
  ● фонетическая замена (10%):  рублей → рублй
  ● вставка filler (30%):       "бюджет ну в размере ..."
  │
  ▼
task_text:  "бюджет ну в размере пятьсот сороктри рублй"
ground_truth: "бюджет ну в размере 543 рублей"
```

### 4 источника данных

| Источник | Как работает | Кол-во |
|----------|-------------|--------|
| **Шаблоны** | 30 контекстных фраз × случайные числа | 7 500 |
| **Группировка** | Контролируемая генерация суммы/перечисления | 6 000 |
| **Порядковые** | 8 шаблонов × числа 1-31 | 3 000 |
| **Новости** | RSS-ленты, предложения с числами → замена цифр на слова | ~100 |

### Что делаем с синтетикой

1. **Валидация парсера** — `make evaluate-synthetic` проверяет accuracy на чистых данных (96.32%). Если падает — значит регрессия.
2. **Расширение словаря** — на синтетике видны паттерны, которые парсер не умеет обрабатывать. Например, склейки слов, необычные комбинации mag.
3. **Контрпримеры для ошибок** — 12 конкретных ошибок из calibration.f можно сгенерировать целенаправленно и проверить, как парсер с ними справляется.
4. **Потенциальное обучение ML** — если решим перейти к seq2seq (ruT5), синтетика станет тренировочным датасетом.

### Архитектура генератора

```
scripts/generate_synthetic.py
  │
  ├── fetch_news_sentences()     ← парсинг RSS + Wikipedia
  │     └── requests + XML/JSON
  │
  ├── number_to_words(n)         ← число 0-999999999 → слова
  │     └── _hundreds() + THOUSAND_TAIL + MILLION_TAIL
  │
  ├── apply_asr_noise(text)      ← симуляция ASR
  │     ├── замена на ASR_ERRORS
  │     ├── склейка слов (15%)
  │     ├── фонетическая замена (10%)
  │     └── пропуск букв (8%)
  │
  ├── generate_from_templates()  ← 30 контекстных шаблонов
  │     └── вставка {num} + filler'ы (30%)
  │
  ├── generate_grouping_examples() ← сумма vs перечисление
  │     ├── sum_patterns[4] (60%)
  │     └── enum_patterns[6] (40%)
  │
  ├── generate_ordinals()        ← порядковые 1-31
  │     └── 8 шаблонов × число → слово
  │
  └── generate_dataset()         ← сборка .feather
        ├── все источники → список dict
        ├── random.shuffle
        └── pl.DataFrame.write_ipc()
```

### Формат данных на диске

```python
data/synthetic.f  (16 500 строк, ~1 MB)
  task_text: str      ← зашумлённый ASR-текст
  ground_truth: str   ← эталон с цифрами
  source: str         ← template / grouping / ordinal / news
  num_type: str       ← cardinal / ordinal
  noise_level: str    ← clean / noisy
```

## Компоненты генерации

### 1. Базовые числа (clean)
Из числа -> словесная форма, используя `num2words` или наш `lexicon`:

| Разряд | Пример | Доля |
|--------|--------|------|
| 1-digit | 5 -> пять | 24% |
| 2-digit | 37 -> тридцать семь | 33% |
| 3-digit | 150 -> сто пятьдесят | 34% |
| 4-digit | 2500 -> две тысячи пятьсот | 5% |
| 5+ digit | 50000 -> пятьдесят тысяч | 4% |

### 2. ASR-шум (apply_asr_noise)
Типы искажений, применяемые к clean-форме:

| Тип | Пример | Вероятность |
|-----|--------|-------------|
| Замена гласных | пятьдесят -> петьсят | 30% |
| Пропуск букв | тысяча -> тыща | 20% |
| Цоканье | пятнадцать -> пятнадцить | 15% |
| Склейка слов | двадцать одно -> двадцатьодно | 15% |
| Редукция | девяносто -> дивяносто | 10% |
| Комбинация | несколько подряд | 10% |

ASR-шум применяется с вероятностью 60-70%, остальные 30% остаются чистыми.

### 3. Группировка (apply_grouping)
Контролируемое создание:

**Сумма (60% группируемых)**:
- сотня + десяток: `двести пятьдесят -> 250`
- тысячи + сотни: `три тысячи четыреста -> 3400`
- тысячи + сотни + десятки: `пять тысяч шестьсот семьдесят -> 5670`
- десятки тысяч: `двадцать пять тысяч -> 25000`
- сложные: `сто пятьдесят тысяч двести -> 150200`

**Перечисление (40% группируемых)**:
- повтор сотен: `двести триста -> 200 300`
- повтор десятков: `двадцать тридцать -> 20 30`
- перечисление единиц: `пять шесть семь -> 5 6 7`
- смешанное: `триста четыреста пятьсот -> 300 400 500`

### 4. Слитные написания (apply_splitting)
```
дветысячи, тристатысяч, пятьсотмиллионов
двестипятьдесят, тристашестьдесят
```
Из существующих раздельных форм склеиваем 30% обратно.

### 5. Контекстные шаблоны (load_templates)
Из calibration.f извлекаем контекстные фразы-обёртки:
```
"бюджет где то {число} рублей"
"примерно {число} показов"
"около {число} процентов"
"на {число} дней"
"в размере {число} рублей"
"суммарно от {число1} до {число2}"
```

### 6. Порядковые числительные
```
первый -> 1, второго -> 2, тринадцатого -> 13
двадцать первый -> 21, сто пятьдесят второй -> 152
```

## Чек-лист реализации

### Шаг 1: Контекстные шаблоны
- [x] Извлечь контекстные фразы из calibration.f
- [x] Выделить маски вида `... {N} ...`
- [x] 30 шаблонов в CONTEXT_TEMPLATES

### Шаг 2: Генератор чистых чисел
- [x] number_to_words: 0 - 999 999 999 (собственная реализация)
- [x] Покрытие: units, teens, tens, hundreds, thousands, millions
- [x] Правильные падежи для тысяч (одна/две тысячи, тысяч)
- [x] number_to_ordinal_words: 1-31

### Шаг 3: ASR-шум
- [x] Словарь замен (из ASR_ERRORS)
- [x] Levenshtein-вариации для неизвестных слов
- [x] Склейка соседних слов (15%)
- [x] Фонетические замены + вставка filler-слов (30%)

### Шаг 4: Группировка
- [x] Генерация суммы: 4 паттерна (сотни+десятки, тысячи+сотни, ...)
- [x] Генерация перечисления: 6 паттернов (200 300, 5 7, ...)
- [x] Пропорция: 60/40

### Шаг 5: Сборка датасета
- [x] Вставка чисел в контекстные шаблоны
- [x] Применение шума
- [x] Формат: task_text, ground_truth, source, num_type, noise_level
- [x] Сохранение в data/synthetic.f (16 500 строк)

### Шаг 6: Валидация
- [x] Проверка: normalize_text(task) == ground_truth (96.32% clean)
- [x] Проверка распределения разрядов (близко к calibration)
- [x] Проверка шаблонов (4 источника: template/grouping/ordinal/news)
- [x] pytest 9/9

### Шаг 7: Реальные данные (Wikipedia + RSS)
- [x] `scripts/fetch_real_data.py` — сбор из Wikipedia API + новостных RSS
- [x] `data/real.f` — 165 пар реальных данных (4-digit: 34%, 1-digit: 29%)
- [x] `make fetch-real` — перезапуск сбора
- [x] `make evaluate-real` — accuracy на реальных данных (83%)

### Шаг 8: Интеграция с Makefile
- [x] `make synthetic` — генерация датасета
- [x] `make train` — обучение на синтетике (7 392 clean строк)
- [x] `make train-quick` — быстрый тест обучения (200 строк, 10 сек)
- [x] `make evaluate-synthetic` — accuracy на синтетике
- [x] `make fetch-real` — сбор реальных данных
- [x] `make evaluate-real` — оценка на реальных данных

## Формат выходных данных

```python
# data/synthetic.f (16 500 rows, ~1 MB)
schema:
  task_text: str      # зашумлённый ASR-текст
  ground_truth: str   # эталонная нормализация
  source: str         # template / grouping / ordinal / news
  num_type: str       # cardinal / ordinal
  noise_level: str    # clean / noisy
```

## Размеры датасетов

| Датасет | Всего строк | Clean (для обучения) | Train (90%) | Test (10%) |
|---------|-------------|----------------------|-------------|------------|
| `data/synthetic.f` | 16 500 | 7 392 | 6 653 | 739 |
| `data/real.f` | 165 | 165 | — | — |
| `data/calibration.f` | 500 | — | — | — |

Обучение (`make train`) использует **7 392 чистых строк** из `synthetic.f`.
Split 90/10: ~6 650 на обучение, ~739 на тест.
Quick mode (`make train-quick`) — 200 строк для проверки пайплайна (~10 сек на CPU).

## Критерии готовности

- [x] Сгенерировано >=15000 строк (16 500)
- [x] Распределение разрядов: 1d=17%, 2d=32%, 3d=32%, 4d=12%, 5d=6%
- [x] Доля группировки ~36% (6000/16500)
- [x] Доля перечисления ~10%
- [x] Доля ASR-шума ~55% (9108/16500)
- [x] accuracy normalize_text на чистых: 96.32%
- [ ] accuracy normalize_text на чистых >= 98% (нужен Levenshtein)
- [x] Валидация без ошибок (pytest 9/9)

## Известные ограничения

1. Готовых ITN-датасетов на Hugging Face нет — все данные генерируем сами
2. Wikipedia API работает, но даёт ~50% предложений без чисел
3. RSS даёт только заголовки — полные статьи парсятся через BS4 (~80 предл.)
4. ~~Нет генерации слитных написаний~~ ✅ Добавлено (две тысячи -> дветысячи)
5. Нет Levenshtein-вариаций для неизвестных слов
6. Реальные данные имеют accuracy 83% из-за ordinals (парсер их верно нормализует, но GT не размечен)

## Ожидаемый эффект на accuracy

| Мера | Текущее | Цель |
|------|---------|------|
| accuracy | 97.6% | 99%+ |
| ошибки группировки | 8 | < 3 |
| пропущенные числа | 2 | < 1 |
| лишние числа | 2 | < 1 |

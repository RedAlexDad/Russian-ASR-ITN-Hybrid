# Report 2026-06-05 19:01:00 MSK

**Branch:** main
**Stats:** 1 file changed, 28 insertions(+), 10 deletions(-)

## What was changed

### Dockerfile: одностадийный → многостадийный

**Stage 1 (builder):** `python:3.12-slim` + pip install --user
- Устанавливает зависимости в /root/.local
- Можно расширить позже компиляцией нативных扩展ний

**Stage 2 (runtime):** свежий `python:3.12-slim`
- Копирует `/root/.local` из builder — pip и setuptools НЕ попадают в финальный образ
- Копирует только исходный код (слои по частоте изменений)

### Преимущества
- pip отсутствует в runtime — меньше поверхность атаки
- setuptools, wheel и ~50 МБ build-мусора не в финальном образе
- При изменении src/ не пересобирается pip install

# Report 2026-06-05 18:26:58 MSK

**Branch:** main
**Stats:** 1 file changed, 4 insertions(+), 4 deletions(-)

## What was changed

### `makefiles/commands.mk`

- Добавлен флаг `-T` во все команды `docker compose exec` (run, evaluate, errors, test)
- Флаг `-T` отключает выделение псевдо-TTY, позволяя запускать команды в неинтерактивном режиме (например, из `make`)

## Problems encountered

- `make test` завершался с ошибкой: `cannot attach stdin to a TTY-enabled container because stdin is not a terminal`
- Причина: `docker compose exec` по умолчанию пытается подключить TTY, но при запуске через `make` интерактивный терминал отсутствует

## How they were solved

- Добавлен флаг `-T` в команды `$(COMPOSE) exec` внутри `makefiles/commands.mk`
- Флаг `-T` (`--no-TTY`) отключает принудительное выделение псевдотерминала, что позволяет выполнять команды в CI/non-interactive режимах

## Notes for the future

- Убедиться, что `make run`, `make evaluate`, `make errors` тоже корректно работают без TTY

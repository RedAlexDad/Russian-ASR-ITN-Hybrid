# Report 2026-06-06 00:00:00 MSK

**Branch:** main
**Stats:** 9 files changed, 280 insertions(+), 45 deletions(-)

## What was added
- `makefiles/mlflow.mk` — цели: mlflow-up, mlflow-down, mlflow-ui, mlflow-clean
- `reports/plan_mlflow_integration.md` — план интеграции

## What was changed
### `scripts/train.py`
- Полная интеграция MLflow: логирование параметров, метрик, артефактов
- MLflowCallback — логи train_loss/eval_loss по шагам
- evaluate_and_report() — accuracy, classification report, confusion matrix
- Сохранение графиков (training_history.png, confusion_matrix.png)
- Prediction samples (первые 15) как текст
- Защита: если MLflow сервер не запущен — обучение идёт без трекинга

### `makefiles/local.mk` + `commands.mk`
- `--mlflow` флаг добавлен по умолчанию в train и train-quick

### `.gitignore`, `requirements.txt`
- +mlflow, +mlflow.db, +mlflow-artifacts

## Workflow
1. `make mlflow-up` — запустить сервер на :5001
2. `make train-local EPOCHS=1 MAX_SAMPLES=500` — обучение с MLflow
3. Открыть http://localhost:5001 — видеть метрики, графики, модель
4. `make mlflow-down` — остановить сервер
